/*
const bill = [125, 555, 44]
const tipArray = []
const totalBill = []

function calcTotal() {
    let tip = 0
    let percentage = 0
    for (let i = 0; i < bill.length; i++){
        if (bill[i] > 50 && bill[i] < 300) { percentage = 0.15 }
        if (bill[i] < 50 || bill[i] > 300) { percentage = 0.2 }
        tip = bill[i] * percentage
        tipArray.push(tip)
        sum = tipArray[i] + bill[i]
        totalBill.push(sum)
    }
    return tipArray
}

resultTwo = totalBill
result = calcTotal()
console.log(result)
console.log(resultTwo)

const mark = {
    fullName: "Mark Johnson",
    height: 1.69,
    weight: 78
}

const john = {
    fullName: "John Markson",
    height: 1.95,
    weight: 92
}

function calcBMI(height, weight){
    let BMI = 0
    BMI = weight / height**2
    return BMI
}

const markBMI = calcBMI(1.96, 78)
const johnBMI = calcBMI(1.95, 92)
console.log(markBMI, johnBMI)

if (markBMI > johnBMI) {console.log(`${mark.fullName} has a higher BMI than ${john.fullName}`)}
if (markBMI < johnBMI) {console.log(`${john.fullName} has a higher BMI than ${mark.fullName}`)}
*/

